{
  "model_config": {
    "arch": "standard_resnet",
    "base_channels": 64,
    "block_type": "basic",
    "depth": 18
  },
  "optim_config": {
    "optimizer": "sgd",
    "base_lr": 0.1,
    "momentum": 0.9,
    "weight_decay": 1e-4,
    "nesterov": true,
    "epochs": 160,
    "scheduler": "multistep",
    "milestones": [
      80,
      120
    ],
    "lr_decay": 0.1,
    "batch_size": 128
  }
}
